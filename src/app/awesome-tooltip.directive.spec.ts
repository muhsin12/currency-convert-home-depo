import { AwesomeTooltipDirective } from './awesome-tooltip.directive';

describe('AwesomeTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new AwesomeTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
